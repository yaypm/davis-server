'use strict';

require('../../setup.js');
const ResponseEngine = require('../../../app/core/response-engine'),
    chai = require('chai'),
    expect = chai.expect;

describe('Tests the response engine controller', function() {
    it('should generate a', function() {
        let intentManager = require('../../../app/core/response-engine/controller/intentManager');
        console.log('test!');
    });
});
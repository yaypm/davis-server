{%- set problems = intentData.scalability.result.problems %}
{
    "text": "There was one infrastructure problem during the past week.",
    "attachments": [
        {% for problem in problems %}
           {% include "shared-templates/problem-summary/en-us/visual/problem-summary.nj" %}{%- if not loop.last %},{% endif %}
        {%- endfor %}
    ]
}
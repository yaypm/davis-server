{%- set problem1 = intentData.scalability.result.problems[0] -%}
{%- set problem2 = intentData.scalability.result.problems[1] -%}
There were two infrastructure problems detected in the past week that I'd like to bring to your attention. The first problem {% if problem1.status == 'OPEN' %}is {{ problem1.rankedImpacts[0].eventType | friendlyEvent() }}, which is actively impacting {{ problem1.rankedImpacts[0] | friendlyEntityName() }}{% else %}was {{ problem1.rankedImpacts[0].eventType | friendlyEvent() }}, which affected {{ problem1.rankedImpacts[0] | friendlyEntityName() }}, but it closed {{ problem1.endTime | friendlyTime(user) }}{% endif %}.  The second one {% if problem2.status == 'OPEN' %}is {{ problem2.rankedImpacts[0].eventType | friendlyEvent() }}, affecting {{ problem2.rankedImpacts[0] | friendlyEntityName() }}{% else %}was {{ problem2.rankedImpacts[0].eventType | friendlyEvent() }}, which affected {{ problem2.rankedImpacts[0] | friendlyEntityName() }}, but was resolved {{ problem2.endTime | friendlyTime(user) }}{% endif %}.
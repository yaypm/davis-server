{%- set problem = intentData.problemDetails.result -%}
This problem started {{ problem.startTime | friendlyTime(user) }} when {{ problem.rankedEvents | last | attr('eventType') | friendlyEvent() }} started impacting {{ problem.rankedEvents | last | attr('entityName') | friendlyEntityName() }}.
{%- set problem1 = intentData.problem.result.problems[0] -%}
{%- set problem2 = intentData.problem.result.problems[1] -%}
There were a couple of problems I would like to bring to your attention.  The first problem {% if problem1.status == 'OPEN' %}is {{ problem1.rankedImpacts[0].eventType | friendlyEvent() }}, which is affecting {{ problem1.rankedImpacts[0] | friendlyEntityName() }}{% else %}was {{ problem1.rankedImpacts[0].eventType | friendlyEvent() }}, which affected {{ problem1.rankedImpacts[0] | friendlyEntityName() }}, but was resolved {{ problem1.endTime | friendlyTime(user) }}{% endif %}.  The second one {% if problem2.status == 'OPEN' %}is {{ problem2.rankedImpacts[0].eventType | friendlyEvent() }}, impacting {{ problem2.rankedImpacts[0] | friendlyEntityName() }}{% else %}was {{ problem2.rankedImpacts[0].eventType | friendlyEvent() }}, which impacted {{ problem2.rankedImpacts[0] | friendlyEntityName() }}, but it closed {{ problem2.endTime | friendlyTime(user) }}{% endif %}.
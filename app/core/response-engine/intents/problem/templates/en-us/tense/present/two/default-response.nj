{%- set problem1 = intentData.problem.result.problems[0] -%}
{%- set problem2 = intentData.problem.result.problems[1] -%}
There are a couple of active problems at the moment.  The first problem is a {{ problem1.rankedImpacts[0].eventType | friendlyEvent() }} which is affecting {{ problem1.rankedImpacts[0] | friendlyEntityName() }}.  The second one is a {{ problem2.rankedImpacts[0].eventType | friendlyEvent() }} impacting {{ problem2.rankedImpacts[0] | friendlyEntityName() }}.
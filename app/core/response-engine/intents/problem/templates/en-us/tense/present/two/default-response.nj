{%- set problem1 = intentData.problem.result.problems[0] -%}
{%- set problem2 = intentData.problem.result.problems[1] -%}
There are a couple of active issues at the moment.  The first problem is a {{ problem.rankedImpacts[0].eventType | friendlyEvent() }} which is affecting {{ problem.rankedImpacts[0] | friendlyEntityName() }}.  The second one is a {{ problem.rankedImpacts[0].eventType | friendlyEvent() }} impacting {{ problem.rankedImpacts[0] | friendlyEntityName() }}.
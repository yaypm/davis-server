{%- set userData = intentData.userActivity.result -%}
{{ ['Hmmm, it looks', 'It appears'] | random }} like there's currently about {{ userData.dayOne.start.value | round }} user actions hitting {{ userData.name | friendlyApplicationName() }}, {% include "intents/userActivity/templates/en-us/shared/userActionDifference.nj" %}.  The majority of your users accessed your site {{ userData.dayOne.max.time | friendlyTime(user) }}, while the least active time was {{ userData.dayOne.min.time | friendlyTime(user) }}.
{%- set problem1 = intentData.problem.result.problems[0] -%}
{%- set problem2 = intentData.problem.result.problems[1] -%}
{%- set problem3 = intentData.problem.result.problems[2] -%}
First off, there {% if problem1.status == 'OPEN' %}is {{ problem1.rankedImpacts[0].eventType | friendlyEvent }}, which is actively impacting {{ problem1.rankedImpacts[0] | friendlyEntityName }}{% else %}was {{ problem1.rankedImpacts[0].eventType | friendlyEvent }}, which affected {{ problem1.rankedImpacts[0] | friendlyEntityName }}, but it closed {{ problem1.endTime | friendlyTime(user) }}{% endif %}.  {% if problem1.hasRootCause %}A root cause was detected for this problem.{% endif %}  Next, there {% if problem2.status == 'OPEN' %}is {{ problem2.rankedImpacts[0].eventType | friendlyEvent }}, affecting {{ problem2.rankedImpacts[0] | friendlyEntityName() }}{% else %}was {{ problem2.rankedImpacts[0].eventType | friendlyEvent }}, which affected {{ problem2.rankedImpacts[0] | friendlyEntityName() }}, but was resolved {{ problem2.endTime | friendlyTime(user) }}{% endif %}.  {% if problem2.hasRootCause %}A root cause was detected for this problem.{% endif %}  Finally, there {% if problem3.status == 'OPEN' %}is {{ problem3.rankedImpacts[0].eventType | friendlyEvent }}, which is actively impacting {{ problem3.rankedImpacts[0] | friendlyEntityName() }}{% else %}was {{ problem3.rankedImpacts[0].eventType | friendlyEvent }}, which affected {{ problem3.rankedImpacts[0] | friendlyEntityName() }}, but it closed {{ problem3.endTime | friendlyTime(user) }}{% endif %}.  {% if problem3.hasRootCause %}A root cause was detected for this problem.{% endif %}
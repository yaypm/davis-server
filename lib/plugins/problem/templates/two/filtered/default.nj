There were two problems detected {{ timeRange | friendlyTimeRange(user) }} affecting {{ appname }}.  The first problem {% if problems[0].status == 'OPEN' %}is {{ problems[0].rankedImpacts[0].eventType | friendlyEvent() }}, which is currently active{% else %}was {{ problems[0].rankedImpacts[0].eventType | friendlyEvent() }}, but it closed {{ problems[0].endTime | friendlyTime(user) }}{% endif %}.  The second one {% if problems[1].status == 'OPEN' %}is {{ problems[1].rankedImpacts[0].eventType | friendlyEvent() }}, which is currently active{% else %}was {{ problems[1].rankedImpacts[0].eventType | friendlyEvent() }}, but was resolved {{ problems[1].endTime | friendlyTime(user) }}{% endif %}.
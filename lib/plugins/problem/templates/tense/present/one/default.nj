{% if problems[0].status == 'OPEN' %}There is {{ problems[0].rankedImpacts[0].eventType | friendlyEvent() }} affecting {{ problems[0].rankedImpacts[0] | friendlyEntityName() }}. This problem started {{ problems[0].startTime | friendlyTime(user) }} and is unfortunately still on going.{% else %}There is {{ problems[0].rankedImpacts[0].eventType | friendlyEvent() }} that's affecting {{ problems[0].rankedImpacts[0] | friendlyEntityName() }}. This problem started {{ problems[0].startTime | friendlyTime(user) }} but was resolved {{ problems[0].endTime | friendlyTime(user) }}.{% endif %}
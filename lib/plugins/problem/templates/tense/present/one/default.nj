{% if problem1.status == 'OPEN' %}There is {{ problem1.rankedImpacts[0].eventType | friendlyEvent() }} affecting {{ problem1.rankedImpacts[0] | friendlyEntityName() }}. This problem started {{ problem1.startTime | friendlyTime(user) }} and is unfortunately still on going.{% else %}There is {{ problem1.rankedImpacts[0].eventType | friendlyEvent() }} that's affecting {{ problem1.rankedImpacts[0] | friendlyEntityName() }}. This problem started {{ problem1.startTime | friendlyTime(user) }} but was resolved {{ problem1.endTime | friendlyTime(user) }}.{% endif %}
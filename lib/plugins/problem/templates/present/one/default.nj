There is {{ problems[0].rankedImpacts[0].eventType | friendlyEvent() }} affecting {{ problems[0].rankedImpacts[0] | friendlyEntityName() }}.  This problem started {{ problems[0].startTime | friendlyTime(user) }} and is unfortunately still on going.

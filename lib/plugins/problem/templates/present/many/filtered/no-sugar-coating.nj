It looks like there are currently {{ totalProblemCount }} problems affecting {{ appname }}. {% if totalProblemCount > 3 %}After giving it some thought, here are my top three.{% endif %} {% include "lib/plugins/shared-templates/top-three-problems-filtered.nj" %}

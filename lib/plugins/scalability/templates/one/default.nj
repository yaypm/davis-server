There was one infrastructure problem {{ timeRange | friendlyTimeRange(user) }}, {% if problem[0].status == 'OPEN' %}caused by a {{ problem[0].rankedImpacts[0].eventType | friendlyEvent() }} affecting {{ problem[0].rankedImpacts[0] | friendlyEntityName() }}, this problem started {{ problem[0].startTime | friendlyTime(user) }} and is unfortunately still on going.{% else %}caused by a  {{ problem[0].rankedImpacts[0].eventType | friendlyEvent() }} that affected {{ problem[0].rankedImpacts[0] | friendlyEntityName() }}, this problem started {{ problem[0].startTime | friendlyTime(user) }} but was resolved {{ problem[0].endTime | friendlyTime(user) }}.{% endif %}

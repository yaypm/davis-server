There was one infrastructure problem {{ timeRange | friendlyTimeRange(user) }}, {% if problems[0].status == 'OPEN' %}caused by a {{ problems[0].rankedImpacts[0].eventType | friendlyEvent() }} affecting {{ problems[0].rankedImpacts[0] | friendlyEntityName() }}, this problem started {{ problems[0].startTime | friendlyTime(user) }} and is unfortunately still on going.{% else %}caused by a  {{ problems[0].rankedImpacts[0].eventType | friendlyEvent() }} that affected {{ problems[0].rankedImpacts[0] | friendlyEntityName() }}, this problem started {{ problems[0].startTime | friendlyTime(user) }} but was resolved {{ problems[0].endTime | friendlyTime(user) }}.{% endif %}
